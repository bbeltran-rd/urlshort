<%= form_with(model: web_address, local: true) do |form| %>
  <% if web_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_address.errors.count, "error") %> prohibited this URL from being saved:</h2>

      <ul>
        <% web_address.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :full_address %>
    <%= form.text_field :full_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
